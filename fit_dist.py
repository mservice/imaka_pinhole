from jlu.astrometry import Transform2D as high_order
from jlu.astrometry.align import jay, align


def stack(cat_lis, ref_cat):
    '''
    matches and stacks a set of position measurements
    only keeps "stars" that are in the reference catalog
    '''

    pass

def fit_dist(dleta_file, order=6, weights=True)
